# CV方式ノイズバジェット設計書

**Document ID**: PQC-CV-NOISE-v2.0
**Status**: Draft
**前提**: DV方式のerasureモデルとは本質的に異なるCV固有のノイズモデル

---

## 1. CVとDVの損失影響の本質的差異

### 1.1 DV方式（FBQC）の損失モデル

```
光子損失 → 検出不能 → erasure (二値的: 検出 or 消失)
         → erasure率 ε = 1 - η
         → 閾値: ε < ε_th (e.g., 24.9% for surface code)
```

### 1.2 CV方式の損失モデル

```
光子損失 → スクイーズド真空への真空ノイズ混入 → スクイージング連続劣化
         → beamsplitterモデル: V_eff = η × V_sqz + (1-η) × V_vac
         → 実効スクイージング σ_eff = −10 × log₁₀(V_eff) [SNU基準]
         → GKP qubitの論理エラー率 p_err = erfc(√π / (4σ)) / 2,  σ=√(σ²)
```

**重要修正**: 旧版の `σ_eff = σ_gen − L(dB)` は**物理的に不正確**。
この公式は `V_eff = V_sqz / η`（古典信号減衰モデル）と等価だが、量子光学では
各損失段階でビームスプリッターモデルにより真空ノイズ (1 SNU) が混入する。
高スクイージング領域 (V_sqz << 1 SNU) では、わずかな損失でも (1−η) >> V_sqz となり、
真空ノイズが支配的になる。例: 0.2dB損失 (η=0.955) で V_sqz=0.05 SNU に対し
(1−η)=0.045 SNU が加わり、スクイージングは 13dB → **10.3dB** に劣化（dB減算の0.2dBではない）。

正しい公式:
```
V_eff = η_total × V_sqz + (1 − η_total)     [SNU, V_vac=1]
σ_eff = −10 × log₁₀(V_eff)                  [dB]

ここで η_total = Π η_i = 10^(−L_total/10)
```

**本質的差異**: DVは二値的（光子が来たか来ないか）、CVは連続的（ノイズがどれだけ加わったか）。CVでは「erasure閾値」ではなく「ノイズ閾値」で議論すべき。

---

## 2. CV方式ノイズバジェット

### 2.1 全経路ノイズ源の列挙

```
[PPLN OPA] ─── σ_gen = 13dB ──────────────────────────────────
    │                                                          │
    │ (1) OPA内部損失      : L₁ = 0.2dB → ΔN₁ = 0.047 SNU   │
    │ (2) OPA→PMF結合損失  : L₂ = 0.3dB → ΔN₂ = 0.071 SNU   │
    ▼                                                          │
[PMF伝搬]                                                      │
    │ (3) PMF伝搬損失 (1m) : L₃ = 0.01dB → ΔN₃ = 0.002 SNU  │
    ▼                                                          │
[EO gate / BS網]                                               │
    │ (4) EO gate          : L₄ = 0.3dB → ΔN₄ = 0.071 SNU   │
    │ (5) BS (×3)         : L₅ = 0.3dB → ΔN₅ = 0.071 SNU    │
    ▼                                                          │
[遅延線]                                                       │
    │ (6) 短遅延 PMF 2m    : L₆ = 0.004dB → ΔN₆ = 0.001 SNU │
    │ (7) 長遅延 PMF 200m : L₇ = 0.070dB → ΔN₇ = 0.016 SNU  │
    ▼                                                          │
[ホモダイン検出]                                                │
    │ (8) 検出効率 99%     : L₈ = 0.04dB → ΔN₈ = 0.010 SNU   │
    │ (9) 電子ノイズ       : ΔN₉ = 0.005 SNU                  │
    ▼                                                          │
[出力] ── σ_eff ──────────────────────────────────────────────
```

SNU = Shot Noise Unit (真空ノイズ = 1 SNU)

**ΔN統一公式**: ΔN = (1-η)/η [SNU], ここで η = 10^(-L_dB/10)。損失L_dBに対し、真空ノイズがbeamsplitterモデルで混入する。

### 2.2 ノイズバジェット表

| # | ノイズ源 | 損失L(dB) | 追加ノイズΔN(SNU) | スクイージング劣化(dB) |
|---|---------|----------|------------------|---------------------|
| 1 | OPA内部損失 | 0.20 | 0.047 | 0.20 |
| 2 | OPA→PMF結合 | 0.30 | 0.071 | 0.30 |
| 3 | PMF伝搬 | 0.01 | 0.002 | 0.01 |
| 4 | EO gate | 0.30 (Option A) / **0** (Option B採用) | 0.071 / **0** | 0.30 / **0** | |
| 5 | BS ×3 | 0.30 | 0.071 | 0.30 |
| 6 | 短遅延PMF | 0.004 | 0.001 | 0.004 |
| 7 | 長遅延PMF 200m (DNANF→PMF変更) | 0.070 | 0.016 | 0.070 |
| 8 | ホモダイン効率 (†QE注記参照) | 0.04 | 0.010 | 0.04 |
| 9 | 電子ノイズ | — | 0.005 | 0.02 |
| 10 | 位相ノイズ×反スクイージング混入 (単一パスOPA 13dB基準) | — | 1.5×10⁻⁵ | **<0.002** |
| 11 | ADC量子化雑音 (14bit) | — | 0.003 | 0.01 |
| 12 | PMF偏波消光比 (>30dB) | — | 0.001 | 0.005 |
| 13 | WDMチャネル間クロストーク (>30dB isolation) | — | <0.001 | <0.005 |
| **14** | **WDM AWG挿入損失** | **0.20** | **0.047** | **0.20** |
| 15 | EO gate消光比 (25dB) | — | ~0.003 | ~0.02 |
| 16 | GAWBS (200m PMF) | — | ~1×10⁻⁴ | ~0.005 |
| **合計** | | **1.12dB (光学#1-8+#14, Option B: EO gate除外)** | **0.288 SNU** | **(旧1.42dBからEO gate 0.30dB除外。Option B採用)** |

**注**:
- WDM AWG(#14): 低損失AWG (NTT-AT 100GHz 8ch) 0.15-0.2dB。0.2dBを設計値とする。
  通信用AWG(0.5dB)では性能27倍劣化のため使用禁止。調達先: NTT-AT, Enablence, 約45万円。
- WDMクロストーク(#13): チャネル間隔100GHz以上で<0.005dB。Phase -1実験で確認。
- 位相ノイズ(#10): **単一パスPPLN OPA（共振器なし）では反スクイージング方向のvariance V_anti = 1/V_sqz = 20 SNU (13dB anti-squeezed)**。
  旧値20dB(V_anti=100)は共振器型OPOの値が混入していた誤り — 単一パスOPAのパラメトリック利得は
  squeezed/anti-squeezed方向に対称: V_sqz×V_anti=1 (最小不確定性状態)。V_sqz=0.05 → V_anti=20。
  テーブル値: V_anti × sin²(0.05°) = 20 × 7.6×10⁻⁷ = 1.5×10⁻⁵ SNU → **<0.002dB（事実上無視可能）**。
  PLL BW≥500kHzでφ_hf<0.05°を保証する限り、位相ノイズは設計制約から完全に脱落。
  RSS位相ノイズ φ_RSS = 0.5° (ファイバ系全体、PLL補正前)。
  PLL BW=500kHz適用後: 残留位相ノイズ φ_residual ≈ 0.05° (05_phase-lock §6計算値0.041°に安全マージン+20%を付加)。
  **PLL未補正時(φ=0.5°)**: V_anti×sin²(0.5°)=20×7.6×10⁻⁵=1.5×10⁻³ SNU。
  Phase 2+ PIC基準(V_eff=0.0813): V_degraded=0.0828 → σ_eff=10.82dB → **劣化~0.08dB**。
  Phase -1 discrete基準(V_eff=0.316): V_degraded=0.318 → σ_eff=4.98dB → **劣化~0.02dB**。
  (旧0.17dBはBWの帯域積分値との混同。単一周波数0.5°では上記が正確。
  **BW=100kHz時の全帯域積分位相ノイズ**: 05_phase-lock §6のφ_RSS=0.5°はPLL補正前値であり、
  BW=100kHz残留時は広帯域積分で~0.17dB相当の劣化が残る — Phase 2+ PIC(理論限界) σ_eff 10.8→10.6dB。)
  PLL BW≥500kHzは依然として推奨だが、PLL失敗(BW=100kHz)時でもσ_eff≈10.7dBでFTQC動作可能。

> **PLL要件検証**: PMF 200m系でのPLL BW≥500kHz達成はPhase -1 T0a最優先実験項目。
> PMF固有の位相ノイズがSMF比2-5倍の場合: φ_res=0.1-0.25° → σ_eff 0.02-0.15dB劣化。
> フォールバック: free-space delay line（位相ノイズ1/10）への切替。
- GAWBS(#16): PMF 200m遅延線での導波音響ブリルアン散乱(GAWBS)。
  PMFではストレスロッドによる弾性モード構造変調により特定GAWBS周波数が抑制され~1×10⁻⁴ SNU。
  (注: 0.01 SNUは単一モードファイバkm級の値。PMF 200mでは散乱長が短く10⁻⁴ SNU級。Shelby+ 1985。)
  温度安定化(±0.01℃)で更に低減可能。劣化0.005dBは保守的上限値。
- **EO gate挿入損失0.30dBの定義**: LNOI MZM変調アーム通過損失（ファイバ結合損失を含まない）。LNOI MZMのfull fiber-to-fiber挿入損失は典型2-3dBだが、本設計ではOPA→EOM→BSがall-fiber spliced接続のため、I/O結合損失は項目#2(OPA結合)に含まれる。EO gate固有の損失はDC arm loss (~0.15dB) + RF modulator core loss (~0.15dB) = 0.30dB。
  **EOM損失リスク (Option A参考値)**: 0.3dBはLNOI MZMの最良ケース。on-chip propagation loss実測がPhase -1確認項目。**Option B採用によりEOMは光路上にないため、本リスクは非該当**。Option Aフォールバック時の参考: 0.5dB到達時にBSモデルでσ_eff約0.5-0.7dB追加劣化。Go/No-Go: EOM固有損失≤0.5dB（Option A時のみ）。
- **配線損失と本テーブルの対応関係**: 12_mechanical.md §2.4で配線損失合計0.64dBと計算されている。
  本テーブルとの対応: #2(OPA結合0.30dB)=#OPA出力FC/APC×2 + #5(BS 0.30dB)=BS融着含む + #3(PMF 0.01dB)。
  合計0.61dBに対し実装値0.64dB → 差分0.03dBは設計マージン(丸めマージン+0.03dB)に含まれる。
  **全ファイバ融着設計**により、FC/APCコネクタはOPA I/O(2箇所)のみ。その他16箇所は全て融着接続(0.02dB/箇所)。
  fiber-to-fiber EOM(2-3dB)の問題は発生しない — EOMはPMFピグテール品(input/output fusion spliced)を使用。
- **EO gate消光比の物理機構**: EO Mach-Zehnder変調器(LNOI)の消光比25dBは、OFF状態でのリーク光が10^(-2.5)=0.32%。このリーク光は未スクイーズ真空(1 SNU)であるため、スクイーズドモードに0.0032 SNUのノイズとして混入。100MHz動作のduty比50%を考慮すると寄与は0.0032×0.5≈0.002 SNU。テーブル値0.003 SNUはduty比変動マージンを含む保守値。
- **LO生成EDFA ASEノイズ**: EO comb + EDFAでLO生成する場合、EDFA後のASE excess photon ratio =
  nsp×(G-1) ≈ 16 (NF=5dB, G=+10dB)。LO 10mW, 検出帯域1GHzでASE/shot≈16:1となりshot-noise limited動作が破壊される。
  **必須対策**: EDFA後にFP etalon (FSR=100GHz, F=50, 透過帯域2GHz) を挿入。
  フィルタ後ASE < 1μW (検出帯域内) → shot noise以下に抑制。
  フィルタ挿入損失 ~0.5dB はLO側のみでありスクイーズド光路に影響なし。
  コスト: FP etalon 約8万円×2ch = 約15万円。ノイズバジェット影響なし。
- WDMチャネル配置: 縮退点(1550nm)片側のみ使用（信号-アイドラー相関回避）。
- **†ホモダイン効率リスク注記**:
  テーブル値QE=99%(0.04dB)は設計目標。市販InGaAs PIN-PD(Thorlabs等)のQEは~95%(=0.22dB損失)。
  **BSモデルによるQE=95%影響評価**:
  Phase 2+ PIC構成(L_base=0.15dB)でQE=95%(+0.18dB): L_total=0.33dB, η=0.927
  V_eff = 0.927×0.0501+0.073 = 0.046+0.073 = 0.119 → σ_eff=**9.2dB** (QE=99%の10.8dBから1.6dB低下)
  p_err = erfc(√π/(4×√(0.119/2)))/2 = erfc(1.822)/2 ≈ 5.3×10⁻³
  → p_err(0.53%)はp_th_eff(1.5%)未満だが、MWPM d=7: p_L≈1.7×10⁻⁴ → 製品仕様(≲10⁻³)は達成。条件付き目標(10⁻⁵, L≤0.17dB)は未達
  **QE=95%ではPIC統合でも製品級FTQC不可能。カスタム高QE PD(≥98%)は必須。**
  **QE=98%ケース追記**: L_base=0.15dBでQE=98%(+0.09dB): L_total=0.24dB, η=0.946
  V_eff = 0.946×0.0501+0.054 = 0.101 → σ_eff=**9.95dB**。p_err≈1.8×10⁻³。MWPM d=7: p_L≈5×10⁻⁶ → 達成。
  **ただしL=0.27dB(現実的)+QE=98%**: L_total≈0.36dB, η=0.920, σ_eff≈9.0dB。MWPM d=7: p_L≈8×10⁻⁵ → 製品仕様(≲10⁻³)は達成だが、条件付き目標(10⁻⁵)は未達。
  **結論**: L=0.27dBバジェットは**QE≥99%前提**。QE=98%の場合はL≤0.20dB必須、またはd=9で対応。
  Phase 1(離散光学)ではQE影響はσ_eff=8.5→7.4dBでbreak-even困難。旧8.8→8.5dB
  **Go/No-Go G6**: Phase 0a終了時にPD QE≥98%達成を確認。QE≥99%は製品L=0.27dBバジェットの前提条件。未達時はd=9+MWPM、またはQD追加。
- **WDMチャネル数制約**: PPLN OPA type-0 PDCのFWHM帯域は~500GHz（片側~250GHz, 50mm導波路）。
  100GHz間隔AWGで最大4-5ch/OPA出力。2 OPA出力で最大8-10ch。ただし帯域端(-3dB)では
  スクイージングが0.5-1dB低下するため、実効的には**中心4ch/OPAが高品質** (>12.5dBスクイージング)、
  端2ch/OPAは品質低下(11.5-12dB)。Portable Max(7ch)は中心4+端3構成で全ch≥11.5dB確保。
  Phase -1 T0aでOPA帯域を実測し、チャネル配置を最適化する。
  **チャネル品質分布**: WDM 8chの実効σ_eff分布は中心4ch≥12.5dB、端4ch~11.5-12.0dB。
  製品仕様(σ_gen≥11.5dB)は最悪チャネルに基づく保守値であり全chで達成。端chの有効GKP品質は
  中心chより~5-10%低下するため、デコーダはチャネル別confidence重みを適用する（Phase 0 FW実装）。

**正しい値 (beamsplitterモデル)**:
- 現行設計 (Option A, WDM込み, L=1.42dB): σ_eff = **5.0dB** (閾値未満)
- 損失削減設計 (Option B, WDMなし, L=0.39dB, 13dB生成): σ_eff = **8.5dB** (閾値境界) V_non-loss全項目合算
- PIC統合現実的 (L=0.27dB, 13dB生成): σ_eff = **≈9.3dB** (製品級FTQC) erfc再計算+V_non-loss修正
- PIC統合理論限界 (L=0.15dB, 13dB生成, V_nl=0): σ_eff = **10.8dB** (理論上限)
旧値 11.5dB/11.7dB は誤った dB減算公式 σ_eff=σ_gen−L による — 廃止

**p_phys保守値の根拠**: Phase 2+ PIC構成 σ_eff=10.8dBでp_phys≈1.0×10⁻³。
設計保守値5×10⁻⁴(+50%)は以下のマージンを包含する:
(a) 有限エネルギーGKP(Δ=0.10-0.15)の追加ノイズ、(b) WDMチャネル品質ばらつき(端ch -0.5dB)、
(c) 動作中温度変動(±0.05℃)による位相ドリフト。11.5-11.75dBの範囲上限値として採用。

**前提条件**: σ_gen=13dBは設計目標値。PPLN導波路OPAの現時点実証値は12.1dB (02_opa-source §1.1)。Phase -1 T0aでσ_gen≥12.5dB達成を確認（Go/No-Go基準）。

**注**: WDM AWGはPhase 2+で再導入予定（PIC統合で低損失化後）。Phase 0-1は単一ch動作。
低損失AWG (NTT-AT製, 0.15-0.2dB) を指定。通信用安価品(0.5dB)は使用不可。

### 2.3 実効スクイージング（beamsplitterモデル）

```
σ_gen = 13.0 dB → V_sqz = 10^(−13/10) = 0.0501 SNU
反スクイージング: ~13dB (単一パスOPAのため対称)

■ 正しいbeamsplitterモデルによる計算:

【現行設計 (Option A: CW OPA + EO gate, WDM AWG込み)】
  全光学損失 L_total = 1.42dB → η_total = 10^(−1.42/10) = 0.720
  V_eff = 0.720 × 0.0501 + (1 − 0.720) = 0.036 + 0.280 = 0.316 SNU
  σ_eff = −10 × log₁₀(0.316) = **5.0 dB** ← 旧値11.5dBから6.5dB低下

  ⚠ 閾値(soft-info 1.5%): p_err < 1.5%に必要なσ_eff ≥ 9.6dB → **閾値未満**
  ⚠ 閾値(postselection 7.5dB): 5.0 < 7.5 → **閾値未満**

  旧公式 σ_eff = 13 − 1.53 = 11.5dB が誤りだった根本原因:
  dB減算は V_eff = V_sqz/η と等価で、真空ノイズ混入を無視している。
  V_sqz = 0.05 SNU に対し (1−η) = 0.28 SNU → ノイズが信号の5.6倍。

【損失削減設計 (Option B: パルスOPA, EO gate除去, WDMなし)】
  損失削減ロードマップ:
    #1 OPA内部: 0.20 → 0.10dB (SiNクラッド最適化)
    #2 OPA結合: 0.30 → 0.10dB (融着ピグテール最適化)
    #3 PMF伝搬: 0.01dB (変更なし)
    #4 EO gate: 0.30 → **0dB (除去: Option Bパルスポンプ採用)**
    #5 BS ×3:  0.30 → 0.10dB (低損失ファイバカプラ選別品)
    #6 短遅延:  0.004dB (変更なし)
    #7 長遅延:  0.07 → 0.04dB (低損失PMF, Nufern PM-1550)
    #8 検出:   0.04dB (変更なし)
    #14 AWG:   0.20 → **0dB (除去: Phase 1まで単一ch動作)**
  ───────────────────────────
  L_total (削減後) = 0.10+0.10+0.01+0+0.10+0.004+0.04+0.04 = **0.39dB**
  η_total = 10^(−0.39/10) = 0.914
  V_eff = 0.914 × 0.0501 + 0.086 + 0.010 = 0.046 + 0.086 + 0.010 = 0.142 SNU  V_non-loss=0.010追加
  σ_eff = −10 × log₁₀(0.142) = **8.5 dB**

  p_err = erfc(√π/(4×√(0.142/2)))/2 = erfc(√π/(4×0.266))/2
        = erfc(1.664)/2 ≈ **9.3×10⁻³** V_non-loss込み再計算

  p_err(0.93%) < p_th_eff(1.5%) → **soft-info閾値超過** ✓
  p_err/p_th = 0.62 → QEC有効だが抑制は弱い (p_L(d=7,MWPM) ≈ 4.4×10⁻³)

【旧Phase 1目標(15dB)は撤廃。Phase 1は13dB/L=0.39dBでbreak-even実証（上記Option Bと同一）。】
  Phase 1 (13dB, L=0.39dB, V_nl=0.010): σ_eff=8.5dB, p_err≈9.3×10⁻³ V_non-loss込み
  UF:   p_err/p_th = 0.93 → p_L(d=7) = 0.07 × 0.93⁴ = **5.2×10⁻²** (break-even未達)
  MWPM: p_err/p_th = 0.62 → p_L(d=7) = 0.03 × 0.62⁴ = **4.4×10⁻³** (break-even境界)
  → Phase 1目標はQEC break-even実証。製品級p_LにはPIC統合(Phase 2+)が必要。

【PIC統合設計 (Phase 2+ 目標) 】
  LNOI/SiN **パッシブPIC** (BSとルーティングのみ。EOM等アクティブ素子はファイバ結合外付け)

  **オンチップ損失**: L_PIC = 0.10dB [カプラ損失は全パス側に計上]
    内訳: SiN導波路 0.05dB (0.01dB/cm×5cm) + BS 0.02dB + マージン 0.03dB
  **全パス損失 (現実的楽観)**: L_total = 0.27dB
    内訳: OPA内部 0.10dB + OPA→PIC直接接合 0.05dB (adiabatic taper) + PICオンチップ 0.10dB + PIC→PD flip-chip 0.02dB
    ※OPA→PIC結合0.05dBはfusion pigtail最適化後の目標値。現状0.10-0.30dB。
  **理論限界 (全条件達成時)**: L_total = 0.15dB (OPA結合ゼロ = モノリシック統合時のみ)
  ※fallback/03_hybrid-pic-design.mdのPIC損失1.5dBは「フル統合PIC(EO込み)」の値であり本設計とは構成が異なる

  【現実的楽観 (L=0.27dB, 製品スペック基準)】
  erfc再計算 + V_non-loss全non-loss項目合算。旧p_err=3.3×10⁻³は
  V_eff=0.107(non-loss除外)用の値をV_eff=0.1124行にコピペした誤り(正: erfc(1.87)/2=4.1×10⁻³)。
  V_non-lossもテーブル§2.2の全non-loss項(#9+#11+#12+#13+#16)合計0.010に更新。

  η_total = 10^(-0.27/10) = 0.9397
  σ_gen = 13dB → V_sqz = 10^(-1.3) = 0.0501
  V_non-loss = 0.010 SNU (#9電子0.005 + #11ADC量子化0.003 + #12PMF偏波0.001 + #13WDMクロストーク0.001 + #16GAWBS0.0001)

  ■ 統一計算チェーン (non-loss noise全項目込み、製品正規値):
    V_eff = η × V_sqz + (1-η) + V_non-loss
          = 0.9397 × 0.0501 + 0.0603 + 0.010
          = 0.0471 + 0.0603 + 0.010 = 0.1174 SNU
    σ_eff = −10 × log₁₀(0.1174) = **9.30dB ≈ 9.3dB**
    σ = √(V_eff/2) = √(0.0587) = 0.2424
    p_err = erfc(√π/(4σ))/2 = erfc(√π/(4×0.2424))/2 = erfc(1.828)/2 ≈ **4.9×10⁻³**
    MWPM p_th = 0.015 (soft-info)
    p_err/p_th = 4.9×10⁻³ / 0.015 = 0.327
    p_L(d=7) = 0.03 × 0.327⁴ = 0.03 × 1.14×10⁻² ≈ **3.3×10⁻⁴** ← 製品仕様正規値(L=0.27dB)

  ■ L=0.22dBターゲット (p_L≲10⁻⁴達成ライン):
    η = 0.9506, V_eff = 0.1071, σ_eff = 9.7dB, p_err ≈ 3.4×10⁻³
    p_err/p_th = 0.226, p_L(d=7) = **7.8×10⁻⁵** ← L≤0.22dBで≲10⁻⁴達成

  ■ 結論: 製品スペック(L=0.27dB, QE≥99%)での正規p_L = **3.3×10⁻⁴ (≲10⁻³)**
    L≤0.22dBで≲10⁻⁴達成。L≤0.17dBで<10⁻⁵達成。理論限界(L=0.15dB, V_nl=0)で6.1×10⁻⁷。

  【理論限界 (L=0.15dB, QE=99%, Δ=0)】
  η = 10^(-0.15/10) = 0.966
  σ_gen = 13dB:
    V_eff = 0.966 × 0.0501 + 0.034 = 0.048 + 0.034 = 0.082 SNU  [V_non-loss=0: 理論限界]
    σ_eff = −10 × log₁₀(0.082) = **10.8 dB** 正確値10.84dB
    p_err = erfc(√π/(4×√(0.082/2)))/2 = erfc(2.186)/2 ≈ **1.0×10⁻³** erfc再計算
    MWPM: p_err/p_th = 0.067 → p_L(d=7) = 0.03 × 0.067⁴ = **6.1×10⁻⁷** ← 設計上限(全条件達成時のみ)

  σ_gen = 15dB:
    V_eff = 0.966 × 0.0316 + 0.034 = 0.031 + 0.034 = 0.065 SNU
    σ_eff = −10 × log₁₀(0.065) = **11.9 dB**
    p_err = erfc(2.465)/2 ≈ **2.4×10⁻⁴**
    MWPM: p_err/p_th = 0.016 → p_L(d=7) = 0.03 × 0.016⁴ = **2.0×10⁻⁹** ← 高性能FTQC

注: PLL BW ≥ 500kHzが前提。BW<100kHzの場合追加劣化~0.17dB。
    Phase -1実験での位相ノイズ実測が最重要確認項目の一つ。
```

**閾値定義の検証課題**:

Stafford-Menicucci-Walshe 2025の閾値7.5dBの正確な定義について3つの解釈がある:
- **解釈A** (post-loss σ_eff基準): 7.5dB = 損失後の実効スクイージング最低値
  → 現設計(5.0dB)は閾値未満。損失削減+PIC化が必須。
- **解釈B** (input squeezing基準、損失はモデルに内包):
  → 13dB > 7.5dBで閾値超。ただしp_Lは損失に大きく依存し、旧主張値より桁違いに悪い。
- **解釈C** (2Dパラメータ空間): squeezing × loss の閾値曲線で判定
  → 単一数値比較は不適切。閾値論文のlossモデルに依存。

**設計上の作業仮定**: 本設計では**解釈Aを採用**する。すなわち閾値7.5dBはpost-loss σ_effに対する
最低要件であり、Phase 2+ PIC現実的(σ_eff≈9.3dB)で+1.8dBマージンを確保。
この解釈は最も保守的であり、他の解釈が正しい場合にはマージンがさらに拡大する方向となる。

**Phase -1必須検証(T0b)**: 閾値論文(Stafford-Menicucci-Walshe 2025 Fig.3)を精読し、
x軸の定義と損失モデルを正確に特定。並行してStim GKP displacement noise + loss channel
シミュレーションで独自に閾値曲線を算出。作業仮定の妥当性を確認する。

**macronode BS混合ノイズの計上状況**:
macronode内のBS網通過時には (1)excess損失由来の真空ノイズ混入 と (2)隣接モードの反スクイージング混入 の2種がある。
- (1) excess損失: §2.2 #5「BS ×3: 0.3dB」として計上済み（50:50分割自体は entanglement生成であり損失ではない）。
- (2) 反スクイージング混入: 適応的測定基底選択により最小化するプロトコルが前提。

---

## 3. GKP品質への影響

### 3.1 σ_eff → GKP忠実度マッピング

GKP符号状態の忠実度は実効スクイージングの関数:

| σ_eff (dB) | σ_eff (linear) | GKP Δ (格子ずれRMS) | F_GKP | p_err (GKP) |
|-----------|----------------|---------------------|-------|-------------|
| 7.5 | 2.37 | 0.42 | 0.82 | 1.8×10⁻² |
| 9.0 | 2.82 | 0.35 | 0.88 | 6.3×10⁻³ |
| 10.0 | 3.16 | 0.32 | 0.91 | 2.5×10⁻³ |
| **11.8** | **3.89** | **0.26** | **0.94** | **2.8×10⁻⁴** |
| 12.0 | 3.98 | 0.25 | 0.95 | 2.0×10⁻⁴ |
| 15.0 | 5.62 | 0.18 | 0.98 | 3.1×10⁻⁷ |

**Taros CV方式 (段階的性能ロードマップ)**:
- **Phase -1/0** (ディスクリート光学, Option B, 13dB, L=0.39dB): σ_eff≈**8.5dB** → p_err≈9.3×10⁻³ (閾値境界)
- **Phase 1** (ディスクリート光学, Option B, 13dB, L=0.39dB): σ_eff≈**8.5dB** → p_err≈9.3×10⁻³ (break-even実証)
- **Phase 2+ 現実的** (PIC統合, 13dB, L=0.27dB): σ_eff≈**9.3dB** → p_err≈4.9×10⁻³ (製品級FTQC)
- **Phase 2+ 理論限界** (PIC統合, 13dB, L=0.15dB): σ_eff≈**10.8dB** → F_GKP≈0.91, p_err≈1.0×10⁻³

### 3.2 QD支援時の追加効果

QD photon subtractionによるスクイージング等価緩和:

| QD数 | 等価スクイージング追加 | σ_eff (等価) | F_GKP | p_err |
|------|---------------------|-------------|-------|-------|
| 0 | 0 | 10.8dB (Phase 2+ PIC) | ~0.91 | 1.0×10⁻³ |
| 2 | +2.7dB等価 | **~13.5dB** (10.8+2.7) | **0.95**† | **3.0×10⁻⁶** |
| 4 | +4dB | 14.8dB | 0.99 | 5.0×10⁻⁷ | 10.8+4

†F_GKP=0.95はQD=2 Step B実用値（QD不可弁別性V=0.95考慮後、04_gkp-protocol.md §3.3準拠）。等価σ_effマッピングの理想値F≈0.97ではなく、QD imperfectionsを反映した実用値を使用。

---

## 4. 表面符号論理エラー率

### 4.1 GKP + 表面符号のスケーリング

**GKP物理エラー率の導出**:

*理論的注記*:
- 公式 `erfc(√π/(4σ))/2` はmacronode BS通過後のnoise variance σ²に対する式。
  教科書の単一モード理想GKP公式 `erfc(√π/(2√2·σ))/2` とは√2の差がある。
  これはmacronode BSネットワークでのnoise folding (×2 variance)を反映。
  導出: Walshe+ 2020, Appendix B (macronode effective noise)。
- A=0.03, p_th=0.59%/1.5%: Noh & Chamberland 2022 Fig.5からの読み取り値。
  soft-info p_th_eff=1.5%は同論文のanalog-info **MWPM**での近似値。
  **注意: Union-Find(UF)デコーダではA≈0.05-0.10, p_th≈0.8-1.2%が妥当
  (Delfosse & Nickerson 2021)。Phase 2+ PIC理論限界 (σ_eff=10.8dB, p_err=1.0×10⁻³) でUF d=7: p_L≈7.0×10⁻⁶(>10⁻⁶)。
  **製品デコーダはMWPM d=7: p_L≈6.1×10⁻⁷。** UFはPhase 0-1実験用。**
  厳密にはA,p_thは同一fitから取るべきだが、p_phys<<p_thの領域では
  AのO(1)変動がp_Lに与える影響は(p/p_th)^4に比べ小さい。
- 有限エネルギーGKP補正: 本設計ではideal GKPを仮定。
  **必須要件: Δ < 0.15** (σ²増加<0.011、σ_eff低下<0.6dB)。
  Δ=0.1(excellent): σ_eff→10.8dB(動作可能)。
  Δ=0.2(good): σ_eff→9.6dB(閾値境界、要d≥9)。
  Δ≥0.3: σ_eff<8dB(動作不能)。Δ≈0.3での追加noise
  (σ²→σ²+Δ²/2≈+0.045)は未計上。Phase -1実験で実測し反映予定。
- p_th=0.59%(保守)はσ_eff≈10dBに対応。7.5dB閾値(postselection)は
  高品質モードのみ使用時にp_thが実効~1.8%に緩和されることを意味する。

```
σ² = (1/2) × 10^(-σ_eff_dB / 10)   [displacement noise variance, post-BS]

beamsplitterモデルによるσ_eff計算結果を使用

■ Phase 1 (Option B, 13dB, L=0.39dB) V_non-loss込み再計算:
  σ_eff = 8.5dB → σ² = (1/2) × 10^(-0.85) = 0.071 → σ = 0.266
  p_err = erfc(√π/(4×0.266))/2 = erfc(1.664)/2 ≈ 9.3×10⁻³

■ Phase 2+ PIC統合 理論限界 (13dB, L=0.15dB):
  σ_eff = 10.8dB → σ² = 0.0412 → σ = 0.203
  p_err = erfc(√π/(4×0.203))/2 = erfc(2.186)/2 ≈ 1.0×10⁻³

■ Phase 2+ 高性能PIC (15dB, L=0.15dB):
  σ_eff = 11.9dB → σ² = 0.0323 → σ = 0.180
  p_err = erfc(√π/(4×0.180))/2 = erfc(2.465)/2 ≈ 2.4×10⁻⁴
```

**表面符号論理エラー率**:
```
p_L(d) = A × (p_err / p_th)^((d+1)/2)

UF soft-info: A_UF = 0.07, p_th_UF = 1.0% (depolarizingモデル外挿)
MWPM soft-info: A = 0.03, p_th_eff = 1.5% (Noh & Chamberland 2022)
```

**Phase 1 (σ_eff=8.5dB, p_err=9.3×10⁻³)** V_non-loss込み再計算:
```
UF:   p_err/p_th = 9.3×10⁻³ / 0.01 = 0.93
  d=5: 0.07 × 0.93³ = 5.6×10⁻² (break-even未達)
  d=7: 0.07 × 0.93⁴ = 5.2×10⁻² (break-even未達)
MWPM: p_err/p_th = 9.3×10⁻³ / 0.015 = 0.62
  d=5: 0.03 × 0.62³ = 7.1×10⁻³ (break-even未達)
  d=7: 0.03 × 0.62⁴ = 4.4×10⁻³ (break-even境界)
→ Phase 1はMWPM d=7でbreak-even境界。製品級p_LにはPIC統合必須。
```

**Phase 2+ PIC統合 理論限界 (σ_eff=10.8dB, p_err=1.0×10⁻³)** erfc再計算:
```
UF: p_err/p_th = 1.0×10⁻³ / 0.01 = 0.10
  d=7: 0.07 × 0.10⁴ = 7.0×10⁻⁶ → 理論限界(L=0.15dB)のみ達成。現実的L=0.27dBではUF~4×10⁻³で製品要件超過→MWPM必須
  d=9: 0.07 × 0.10⁵ = 7.0×10⁻⁷ → 製品要件達成 ✓

**MWPM (製品デコーダ第一選択)**:
  p_err/p_th = 1.0×10⁻³ / 0.015 = 0.067
  d=5: 0.03 × 0.067³ = 9.1×10⁻⁶
  d=7: 0.03 × 0.067⁴ = **6.1×10⁻⁷** → **製品要件達成** ✓
  d=9: 0.03 × 0.067⁵ = 4.1×10⁻⁸
```

**Phase 2+ 高性能PIC (σ_eff=11.9dB, p_err=2.4×10⁻⁴)** erfc再計算:
```
MWPM: p_err/p_th = 2.4×10⁻⁴ / 0.015 = 0.016
  d=7: 0.03 × 0.016⁴ = **2.0×10⁻⁹** → 高性能FTQC
```

### 4.2 論理エラー率予測（beamsplitterモデル）

| Phase | 構成 | σ_eff | p_err | d=7 p_L (UF) | d=7 p_L (MWPM) |
|-------|------|-------|-------|-------------|---------------|
| 現行設計 | Option A, WDM, L=1.42dB | **5.0dB** | 0.15 | **閾値未満** | **閾値未満** |
| Phase 1 | Option B, 13dB, L=0.39dB | **8.5dB** | 9.3×10⁻³ | 5.2×10⁻² | 4.4×10⁻³ |
| **Phase 2+ PIC** | PIC, 13dB, L=0.15dB | **10.8dB** | 1.0×10⁻³ | 7.0×10⁻⁶ | **6.1×10⁻⁷** |
| Phase 2+ 高性能 | PIC, 15dB, L=0.15dB | **11.9dB** | 2.4×10⁻⁴ | 2.8×10⁻⁸ | **2.0×10⁻⁹** |

**結論**:
- **ディスクリート光学（Phase 0-1）**: 13dB+損失削減(L=0.39dB)でσ_eff=8.5dB。MWPM d=7でbreak-even境界。
  p_err/p_th=0.62で指数的抑制の入口。製品級p_Lには不足。
- **PIC統合現実的（Phase 2+, L=0.27dB）**: σ_eff≈9.3dB、p_L(d=7,MWPM)≈**3.3×10⁻⁴** (≲10⁻³)。L≤0.22dBで≲10⁻⁴達成。
- **PIC統合理論限界（L=0.15dB）**: σ_eff=10.8dB、p_L≈6.1×10⁻⁷で十分なマージン。
- **製品級FTQCの鍵はスクイージング向上ではなく損失削減**。

> **UF soft-info暫定注記**: A_UF=0.07, p_th_UF=1.0%はdepolarizingモデルからの外挿。
> GKP displacement noiseでの検証はPhase -1 T0b必須項目。

### 4.3 DV-FBQC方式との性能比較（更新）

| 指標 | CV (QD=0, d=7) | CV (QD=2, d=5) | DV v5.0 (d=6) | DV R6 (d=12) |
|------|---------|---|---------|---|
| p_L (Phase2+ PIC, MWPM) | **6.1×10⁻⁷** (UF: 7.0×10⁻⁶>10⁻⁶) | ~10⁻¹⁰ | ~10⁻⁶ | ~10⁻⁴ |
| 閾値マージン (Phase2+ PIC) | +3.3dB (postsel) | +6.1dB | +14pt | +7pt |
| 物理リソース | OPA8本+ホモダイン2ch | +QD2+SNSPD4 | QD24+SNSPD500 | QD80+SNSPD3000 |
| **確実性** | **高** | **最高** | 中 | 中 |

---

## 5. DV方式 `05_loss-budget/` との対応表

| DV概念 | CV対応概念 | Tarosファイル |
|--------|----------|-------------|
| end-to-end η | 実効スクイージング σ_eff | 本文書 §2 |
| erasure率 ε | GKP物理エラー率 p_err | 本文書 §3 |
| erasure閾値 ε_th | スクイージング閾値 σ_th | 本文書 §3 |
| fusion成功率 | (該当なし — fusionなし) | — |
| depolarizing率 | 位相ロック誤差由来 p_depol | 本文書 §2.2 #10 |
| SNSPD検出効率 | ホモダイン量子効率 | 本文書 §2.2 #8 |

---

## 6. 感度分析

### 6.1 各パラメータの論理エラー率への寄与

σ_eff = 10.8dB (Phase 2+ PIC統合理論限界基準) をベースラインとし、各パラメータを±で変動:

| パラメータ | ベースライン | -1σ変動 | p_L変化(d=7) | 感度 |
|-----------|------------|---------|-------------|------|
| OPA生成スクイージング | 13dB | 12dB | ×30 | **最高** |
| OPA→PMF結合損失 | 0.3dB | 0.5dB | ×5 | 高 |
| BS網損失 | 0.3dB | 0.5dB | ×5 | 高 |
| PMF遅延線損失 | 0.070dB | 0.10dB | ×1.2 | 低 |
| ホモダイン効率 | 99% | 97% | ×3 | 中 |
| 位相ロック精度 | ±0.5° | ±2° | ×8 | 高 |

**最重要パラメータ: OPA生成スクイージング。** 1dBの低下で論理エラー率が30倍悪化。PPLN OPAの品質管理が最優先。

### 6.2 量子重力デコヒーレンス寄与

| デコヒーレンス源 | 寄与 | 根拠 |
|---------------|------|------|
| Diósi-Penrose重力崩壊 | **0** | 光子質量 $m=0 \implies E_G=0 \implies \tau=\infty$ |
| Oppenheim確率的時空揺らぎ | **0** | 結合 $\propto Gm=0$ |
| Schrödinger-Newton非線形項 | **0** | $V_{SN} \propto Gm^2 = 0$ |

**光子ベースのCV量子計算は、あらゆる量子重力理論が予言するデコヒーレンスから原理的に免疫である。** 唯一の基本的制約はOPA生成スクイージングと光学損失のみ。詳細(レガシー参考): `../archive/14_future-vision/09_quantum-gravity-immunity.md`

---

## 7. `design/13_performance.md` への更新事項

暫定性能モデルからの更新履歴（最終値は§2-4を参照）:

| 項目 | 旧 (dB減算) | **BSモデル** | 根拠 |
|------|-----------|---------------|------|
| σ_eff (現行設計, L=1.42dB) | 11.5dB (誤り) | **5.0dB** (閾値未満) | BSモデル: η×V_sqz+(1−η) |
| σ_eff (Phase 1, L=0.39dB, 13dB) | — | **8.5dB** (break-even境界) | 損失削減 |
| σ_eff (Phase 2+ PIC, L=0.15dB) | — | **10.8dB** | PIC統合で製品級 |
| p_L (Phase 2+ PIC, d=7) | 4.4×10⁻⁷ (旧UF) | **6.1×10⁻⁷ (MWPM)** / 7.0×10⁻⁶ (UF) | MWPM必須 |

**核心**: 製品級FTQC(p_L≲10⁻³)にはPIC統合（現実的L≤0.27dB）+MWPM d=7が必須（p_L≈3.3×10⁻⁴）。L≤0.17dBでp_L<10⁻⁵達成。ディスクリート光学(Phase 1)ではbreak-even実証まで。

---

*本文書はCV方式固有のノイズバジェットを構築し、DV方式のerasureモデルとの対応関係を明確化したものである。正しいbeamsplitterモデルに全面改訂。現行ディスクリート光学(L=1.42dB)ではσ_eff=5.0dBで閾値未満。損失削減(Option B, L=0.39dB, 13dB)でσ_eff=8.5dB(break-even境界)。PIC統合現実的(L=0.27dB)でσ_eff≈9.3dB, p_L(d=7,MWPM)≈3.3×10⁻⁴。PIC統合理論限界(L=0.15dB)でσ_eff=10.8dB, p_L≈6.1×10⁻⁷。erfc再計算+V_non-loss全項目合算*
