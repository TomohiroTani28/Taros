# CV+ハイブリッド方式 性能予測モデル

**Document ID**: PQC-PERF-CV-v3.6
**Status**: 確定版 (erfc再計算 + V_non-loss=0.010 SNU補正反映)
**前提**: `design/06_noise-budget.md` + `design/03_tdm-cluster.md` + `design/02_opa-source.md`

---

## 1. 性能モデル概要

暫定版から確定版への主要変更:
- bow-tie OPO → PPLN導波路OPA: σ_gen = 12dB → **13dB** (Phase 1/2+共通。旧15dB要求は撤廃)
- **σ_eff公式修正**: 旧式 σ_eff = σ_gen − L(dB) は物理的に不正確。正しくは **V_eff = η×V_sqz + (1−η), σ_eff = −10×log₁₀(V_eff)**（ビームスプリッタモデル）
- **Option B（パルスポンプOPA）を推奨ベースラインに変更**: EO gate 0.3dB損失排除
- フェーズ別実効スクイージング:
  - Phase 1 (離散光学系, 13dB, L=0.39dB): **σ_eff = 8.5dB** (break-even実証) V_non-loss補正
  - Phase 2+ PIC (現実的, 13dB, L=0.27dB): **σ_eff ≈ 9.3dB** (non-loss込み), p_L(d=7, MWPM) ≈ **3.3×10⁻⁴** (製品スペック: p_L≲10⁻³) erfc再計算 + V_non-loss=0.010 SNU補正
  - Phase 2+ PIC (理論限界, 13dB, L=0.15dB): σ_eff = 10.8dB, p_L ≈ 6.1×10⁻⁷ (全条件達成時のみ)
- 旧値 σ_eff = 11.5dB はdB減算による過大評価（廃止）

---

## 2. パラメータ空間（確定版）

### 2.1 主要パラメータ

| パラメータ | 記号 | 暫定 | **確定** | 根拠 |
|-----------|------|---------|-------------|------|
| 生成スクイージング | σ_gen | 12dB | **13dB (Ph1/Ph2+ 共通)** | PPLN OPA (NTT) Ph1 15dB要求を撤廃 |
| 光学損失 | L_opt | 2dB | **0.39dB (Ph1離散) / 0.15dB (Ph2+ PIC)** | Option B (EO gate排除), CVノイズバジェット |
| 実効スクイージング | σ_eff | 10dB | **8.5dB (Ph1) / ≈9.3dB (Ph2+ 現実的, L=0.27dB) / 10.8dB (理論限界)** | V_eff=η×V_sqz+(1−η)+V_non-loss, σ_eff=−10log₁₀(V_eff)  V_non-loss=0.010 SNU補正 |
| QD不可弁別性 | V | 0.95 | 0.95 | InP QD目標（不変） |
| QD数 | N_QD | 2 | **0（基本）/ 2（拡張）** | QDなしで要件達成 |
| GKP忠実度 | F_GKP | 0.93 | **0.87-0.91（QD=0, Phase依存）/ 0.95（QD=2, Step B）/ 0.98（QD=4, Step C）** | `04_gkp-protocol.md` §2-4参照。Phase・QD数依存で変動 |
| 表面符号距離 | d | 7 | **5-7** | d=5で十分な場合あり |
| 論理qubit数 | N_L | 10-50 | **10-1,000+ (逐次†)** | TDM時間方向拡張 |

### 2.2 パラメータ依存チェーン（確定版）

```
フェーズ別パラメータチェーン (ビームスプリッタモデル: V_eff = η×V_sqz + (1−η)):

Phase 1:  σ_gen=13dB, L=0.39dB → η=0.914 → σ_eff=8.5dB  → p_L(d=7,MWPM)~4.4×10⁻³ (break-even実証) V_non-loss補正
Phase 2+ (現実的): σ_gen=13dB, L=0.27dB → η=0.940 → σ_eff≈9.3dB (non-loss込み) → p_L(d=7,MWPM) ≈ 3.3×10⁻⁴ erfc再計算+V_non-loss=0.010
Phase 2+ (理論限界): σ_gen=13dB, L=0.15dB → η=0.966 → σ_eff=10.8dB → p_L(d=7,MWPM) ≈ 6.1×10⁻⁷ [全条件達成時]
                                                    ↑
                                              N_QD=2で等価改善 → p_L大幅低減
旧値 σ_eff=11.5dB (dB減算) は廃止
```

---

## 3. 論理エラー率モデル（確定版）

### 3.0 未検証仮定と検証計画

以下の仮定はLevel 1 Stimシミュレーション(約840万円, 2ヶ月)で検証予定:

| 仮定 | 根拠 | リスク | 検証方法 |
|------|------|--------|----------|
| UF A=0.07, p_th=1.0% (GKPモデル) | Delfosse+ 2021 (depolarizing転用) | GKP固有ノイズでA,p_thが異なる可能性 | GKP変位ノイズStimシミュ |
| soft-info p_th改善1.0→1.5% | Noh+ 2022 (standard SC) | macronode TDM構造での有効性未確認 | macronode Stimシミュ |
| 適応的基底選択(FF 2-3モード遅延) | Walshe+ 2020 | 有限FF遅延での反スクイージング混入 | macronode回路シミュ |
| 独立ノイズ仮定 | — | 共通ポンプRINで相関→閾値低下 | 相関ノイズStimシミュ |

**フォールバック**: UF未検証時はMWPM soft-info (A=0.03, p_th=1.5%, 文献確立済み)で6.1×10⁻⁷達成可能。

#### 未検証仮定の拡充リスト

| # | 仮定 | リスク | 検証方法 | FAIL時の影響 |
|---|------|--------|----------|-------------|
| U1 🔴 | 有限エネルギーGKP Δ < 0.12 | 光学系で未実証 | G-EXP1: postselection Δ実測 | Δ=0.15→p_L 100倍悪化 |
| U2 🔴 | OPA σ_gen ≥ 13dB (CW) | 現12.1dB、+0.9dBギャップ | NTT共同実験 | 12dB時 p_L→2×10⁻⁵ |
| U3 🟡 | 独立ノイズ仮定 | 共通ポンプRINで相関 | RIN実測 + 相関Stimシミュ | RIN−140dB/Hz時 p_th 20%低下 |
| U4 🟡 | UF soft-info p_th = 1.0% | depolarizing転用、GKP未確認 | GKP変位ノイズStimシミュ | p_th=0.8%時 p_L→1.1×10⁻⁶ |
| U5 🟡 | soft-info MWPM p_th = 1.5% | macronode TDM未確認 | macronode Stimシミュ | MWPM値が10倍悪化 |
| U6 🟢 | 適応的基底選択(FF 2-3モード遅延) | 有限FF遅延での混入 | macronode回路シミュ | σ_eff 0.1-0.3dB劣化 |
| U7 🟢 | twirling近似の妥当性 | q/p非対称との乖離2-10倍 | 非twirling Stimシミュ | p_phys 2-3倍増加 |

### 3.1 スケーリング則

Noh & Chamberland (2022) + Stafford-Menicucci-Walshe (2025):

```
p_L(d, σ_eff, N_QD) ≈ A × (p_phys / p_th)^((d+1)/2)

p_phys(σ_eff) = erfc(√π / (4σ)) / 2
  σ = √(V_eff / 2)  [GKP変位ノイズ標準偏差, SNU基準, macronode BS後]
  V_eff = η×V_sqz + (1−η) [ビームスプリッタモデル]
  σ_eff_dB = -10 log₁₀(V_eff) [スクイージングレベルの定義]
  ※ σ = √(0.5 × 10^(-σ_eff_dB/10)) と同値。V_eff = 10^(-σ_eff_dB/10)
  ※ 検算: σ_eff=10.8dB → V_eff=0.0832 → σ=0.2039 → erfc(√π/(4×0.2039))/2 = erfc(2.17)/2 ≈ 1.0×10⁻³ ✓

パラメータ (06_noise-budget.md §4.1準拠):
  A = 0.03 (soft-info MWPM prefactor, Noh & Chamberland 2022)
  p_th = 0.59% (conservative, hard MWPM) / p_th_eff = 1.5% (soft-info MWPM)

  **デコーダ依存性注記**:
  - 上記A, p_thはMWPMデコーダのフィッティング値。Union-Find(UF)使用時:
    - UF prefactor: A_UF ≈ 0.05-0.10 (Delfosse & Nickerson 2021)
    - UF soft-info閾値: p_th_UF ≈ 0.8-1.2% (MWPM比60-80%)
  - UF使用時のp_L推定 (A=0.07, p_th=1.0%, Phase 2+ σ_eff=10.8dB, p_err≈1.0×10⁻³):
    d=7: 0.07×(1.0×10⁻³/0.01)⁴ = 0.07×(0.1)⁴ = 0.07×10⁻⁴ ≈ **7.0×10⁻⁶**
  - MWPM使用時のp_L推定 (A=0.03, p_th=1.5%, Phase 2+ p_err≈1.0×10⁻³):
    d=7: 0.03×(1.0×10⁻³/0.015)⁴ = 0.03×(0.0667)⁴ ≈ 0.03×2.0×10⁻⁵ ≈ **6.1×10⁻⁷**
  - **製品要件p_L<10⁻³。理論限界(L=0.15dB)ではUF 7.0×10⁻⁶<10⁻⁵で達成するが、現実的(L=0.27dB)ではUF~4×10⁻³で超過。製品デコーダはMWPM d=7を第一選択。**
  - MWPMフォールバック(+160ns): 主張値達成可能だがレイテンシ増加
  - **統計的不確かさ注記**: p_L~10⁻⁷をStim 10⁷ shotsで測定した場合、期待エラー数~1個。95% CI: [0.03, 5.6]×10⁻⁷(相対不確かさ>500%)。上記値はオーダー推定(~10⁻⁷)としてのみ有効。統計的に有意な検証には10⁹ shots以上が必要(Phase -1 T0b計画)。

フェーズ別 (ビームスプリッタモデル):
  Phase 1 (13dB, L=0.39dB):  σ_eff = 8.5dB  → p_err ≈ 9.3×10⁻³ → p_L(d=7,MWPM) ≈ 4.4×10⁻³ → break-even実証 V_non-loss補正
  Phase 2+ PIC (13dB, L=0.27dB): σ_eff ≈ 9.3dB (non-loss込み) → p_err ≈ 4.9×10⁻³
    soft-info (MWPM): p_err/p_th ≈ 0.33 → p_L(d=7) ≈ **~3.3×10⁻⁴** (製品スペック: p_L≲10⁻³)
    soft-info (UF):   p_err/p_th_UF ≈ 0.49 → p_L(d=7) ≈ ~4×10⁻³ (製品要件未達→MWPM必須)
  理論限界 PIC (13dB, L=0.15dB, QE=99%, Δ=0): σ_eff=10.8dB → p_L(d=7,MWPM) ≈ 6.1×10⁻⁷ (全条件達成時の設計上限)
  Phase 2+ PIC (15dB, L=0.15dB): p_err ≈ 2.4×10⁻⁴ → p_L(d=7,MWPM) ≈ 2.0×10⁻⁹ (将来目標)

  旧値 (dB減算, 廃止):
  ~~σ_eff = 11.5dB → σ=0.188 → p_phys = 5.0×10⁻⁴~~

σ_eff等価 ≈ 13.5dB (QD=2, 10.8+2.7; 旧14.2dBは11.5dB基準) → p_phys = 3.2×10⁻⁶ (QD photon subtraction効果)
```

### 3.2 性能予測表

**製品デコーダ: MWPM (510ns@400MHz)**。現実的条件(L=0.27dB)ではUF d=7 p_L~4×10⁻³>10⁻³のため製品要件未達。UFはPhase 0-1実験用ベースライン(350ns@400MHz)。

| 構成 | σ_eff | N_QD | F_GKP | d | p_L (UF soft-info)† | p_L (MWPM soft-info) | 判定 |
|------|-------|------|-------|---|------|------|--------|
| **Phase 1 離散** | **8.5dB** | 0 | — | 7 | ~10⁻² | ~4.4×10⁻³ | **break-even実証** |
| **Phase 2+ PIC 現実的** | **≈9.3dB** | **0** | — | **7** | ~4×10⁻³ | **~3.3×10⁻⁴** | **製品スペック(p_L≲10⁻³)** |
| Phase 2+ PIC 理論限界 (Δ=0,QE99%) | 10.8dB | 0 | — | 7 | ≈7.0×10⁻⁶ | ≈6.1×10⁻⁷ | 設計上限(全条件達成時) |
| ~~旧: CVのみ (dB減算, 廃止)~~ | ~~11.5dB~~ | ~~0~~ | ~~0.93~~ | ~~7~~ | ~~4.4×10⁻⁷~~ | ~~3.7×10⁻⁸~~ | ~~廃止~~ |
| QD支援 (QD=2, Phase 2+) | ~13.5dB* | 2 | 0.95‡ | 5 | **~10⁻¹⁰** | **~10⁻¹¹** | 十分な余裕 |
| QD支援 (QD=2, Phase 2+) | ~13.5dB* | 2 | 0.95‡ | 7 | **~10⁻¹⁴** | **~10⁻¹⁵** | 設計上限 |

†UF soft-info: A_UF=0.07, p_th_UF=1.0%はDepolarizing noiseモデル（Delfosse & Nickerson 2021）からの外挿値。GKP displacement noise channelでの検証はPhase -1 T0b必須項目。d=3,5の値はシミュレーション由来の有限サイズ補正を含み、漸近公式A×(p/p_th)^((d+1)/2)より~30%大きい。d=7は漸近式と一致。
**未検証前提注記**: 上記UF soft-info p_th=1.0%はGKPノイズモデル(Gaussian displacement channel)を
前提とする。macronode BS網通過後の実効ノイズが単純displacement channelで記述できることは
Stafford-Menicucci-Walshe 2025で示唆されているが、光学実装での検証は未完了。
実効p_thが0.8%の場合: p_L(d=7)≈0.07×(5×10⁻⁴/0.008)⁴=0.07×1.5×10⁻⁵=**1.1×10⁻⁶** (Level A境界)。
Phase -1 T0b Stimシミュレーションで実効p_thを確定予定(目標: 0.9%以上でGo)。
*QD支援σ_eff等価値は実σ_eff 10.8dB (Phase 2+) + QD等価+2.7dB ≈ 13.5dB
‡F_GKP=0.95はQD=2 Step B実用値（QD不可弁別性V=0.95考慮後、04_gkp-protocol.md §3.3準拠）。等価σ_eff=13.5dBの理想マッピングではF≈0.97だが、QD imperfectionsにより0.95に低下。旧値0.98はQD=4 Step C（04_gkp-protocol.md §4.3）の値が誤適用されていた。

**設計検証結果 (ビームスプリッタモデル)**: CVのみ（QD=0）でもd=7で:
- **Phase 1 (離散, 13dB, L=0.39dB)**: σ_eff=8.5dB → p_L~4.4×10⁻³ → break-even実証向け（V_non-loss補正）
- **Phase 2+ PIC 現実的 (13dB, L=0.27dB)**: σ_eff≈9.3dB → **p_L(MWPM) ≈ 3.3×10⁻⁴ → 製品スペック（p_L ≲ 10⁻³）** (erfc再計算+V_non-loss=0.010 SNU。L≤0.17dBでp_L<10⁻⁵)
- Phase 2+ PIC 理論限界 (13dB, L=0.15dB, QE=99%, Δ=0): σ_eff=10.8dB → p_L(MWPM) ≈ 6.1×10⁻⁷ (設計上限、全条件同時達成時)
- **製品デコーダはMWPM d=7を第一選択。** UFはPhase 0-1実験用ベースライン
- **旧値 σ_eff=11.5dB, p_L=4.4×10⁻⁷ はdB減算による過大評価（廃止）**

> erfc再計算+V_non-loss修正: 理論限界(15dB,L=0.15dB): p_err≈2.4×10⁻⁴, p_L(MWPM)≈2.0×10⁻⁹。Table 3.1 p_err値は全てerfc(√π/(4σ))/2で検算済み。

### 3シナリオ σ_eff導出根拠

各シナリオのσ_eff実効値は以下の式で計算 (**ビームスプリッタモデル**):

**基本式**: V_eff = η × V_sqz + (1−η) + V_Δ + V_RIN
**σ_eff(dB)** = −10 × log₁₀(V_eff)

- η = 10^(−L_opt/10): 全光学透過率
- V_sqz = 10^(−σ_gen/10): 生成スクイーズド分散
- V_Δ ≈ Δ²: 有限エネルギーGKPによる追加分散
- V_RIN ≈ ρ×(d-1)×V_sqz: TDM相関ノイズ分散

> **注記**: 旧式 σ_eff = σ_gen − L_opt − L_Δ − L_RIN (dB減算) は物理的に不正確。
> 損失が大きい場合、dB減算は実効スクイージングを大幅に過大評価する。

楽観 (Phase 2+ PIC): σ_gen=13, L=0.15, Δ=0.08, RIN −155 → σ_eff≈10.5dB
現実 (Phase 2+ PIC): σ_gen=13, L=0.15, Δ=0.12, RIN −150 → σ_eff≈9.8dB
保守 (Phase 1 離散): σ_gen=13, L=0.39, Δ=0.15, RIN −145 → σ_eff≈7.8dB 旧15dB→13dB

### 3シナリオ性能予測

| シナリオ | σ_gen | L_opt | Δ | ポンプRIN | σ_eff実効 | p_phys | d=7 p_L(UF) | d=7 p_L(MWPM) | 判定 |
|---------|-------|-------|---|----------|----------|--------|-------------|---------------|------|
| **楽観 (Ph2+ PIC)** | 13dB | 0.15dB | 0.08 | −155dB/Hz | 10.5dB | 1.5×10⁻³ | 3.5×10⁻⁵ | 3.0×10⁻⁶ | p_L(MWPM)~10⁻⁶、製品要件達成 |
| **現実 (Ph2+ PIC)** | 13dB | 0.15dB | 0.12 | −150dB/Hz | 9.8dB | 3.1×10⁻³ | 6.3×10⁻⁴ | 5.4×10⁻⁵ | MWPM必須、UF閾値超 |
| **保守 (Ph1 離散)** | 13dB | 0.39dB | 0.15 | −145dB/Hz | 7.8dB | 1.5×10⁻² | >1 (閾値超) | ~2.8×10⁻² | MWPM限界、UF不可 旧15→13dB |
| **Phase 2+ PIC現実的** | 13dB | 0.27dB | 0.10 | −150dB/Hz | ≈9.3dB† | 4.9×10⁻³ | ~4×10⁻³ | **~3.3×10⁻⁴** | 製品スペック(p_L≲10⁻³) erfc再計算+V_non-loss=0.010 SNU |
| 参考: Ph2+ 理想(Δ=0) | 13dB | 0.15dB | 0 | — | 10.8dB | 1.0×10⁻³ | 7.0×10⁻⁶ | 6.1×10⁻⁷ | 理論上限値 |
| 参考: Ph1 理想(Δ=0) | 13dB | 0.39dB | 0 | — | 8.5dB | 9.3×10⁻³ | ~10⁻² | ~4.4×10⁻³ | break-even V_non-loss補正 |
| ~~旧 (dB減算, 廃止)~~ | ~~13dB~~ | ~~1.53dB~~ | ~~0~~ | — | ~~11.5dB~~ | ~~4.3×10⁻⁴~~ | ~~4.4×10⁻⁷~~ | ~~3.7×10⁻⁸~~ | ~~廃止~~ |

> **注**: 製品スペックはp_L≲10⁻³(MWPM d=7)。Phase 2+ PIC現実的(L=0.27dB, non-loss込み)ではMWPM d=7で**~3.3×10⁻⁴**（erfc再計算+V_non-loss=0.010 SNU）、UF d=7で~4×10⁻³(超過)。**製品デコーダはMWPMを第一選択。** 理論限界(L=0.15dB, Δ=0)ではMWPM 6.1×10⁻⁷で十分なマージン。L≤0.17dB + Δ<0.12 + QE≥99% でp_L<10⁻⁵達成可能。
>
> **3シナリオ表の計算方法**: 楽観/現実/保守の3行ではσ_effはΔ・RIN込みの実効値（V_eff = η×V_sqz + (1−η) + V_Δ + V_RIN で計算）。p_phys=erfc(√π/(4σ))/2, σ=√(V_eff/2)で変換。p_L=A×(p_phys/p_th)^((d+1)/2)。MWPM: A=0.03, p_th=1.5%。UF: A=0.07, p_th=1.0%。保守シナリオではp_phys≈1.5%がMWPM閾値近傍、UF閾値超過となる点に注意。
> †**Phase 2+ PIC現実的行（L=0.27dB）の計算方法は上記3シナリオと異なる**: σ_eff≈9.3dBは06_noise-budget §2.3のV_non-loss=0.010SNU（電子ノイズ+ADC+PMF+WDM+GAWBS）込み計算値であり、Δ・RINは未包含。Δ=0.10, RIN=−150の追加効果は06_noise-budget §4で別途評価（Δ分~0.005SNU, RIN分~0.002SNU）。**全込みでは≈9.0-9.1dB相当、p_err≈5.5-6.5×10⁻³、p_L(MWPM d=7)≈5-9×10⁻⁴**（製品スペックp_L≲10⁻³は達成するがマージン縮小）。Δ・RINの精密値はPhase -1実測で確定するため、本表ではV_non-loss包括値9.3dBを製品スペック基準とする。
>
> **対外資料注記**: Phase -1実験前の対外資料ではPhase 2+ PIC現実的行（p_L~3.3×10⁻⁴, MWPM）を基準に提示すること。

### TDM相関ノイズの影響推定

TDM方式では全モードが同一CWポンプから生成されるため、ポンプRINが共通ノイズとして混入する。

- DFB+TA典型RIN: −150 dB/Hz → 隣接モード相関 ρ ≈ 0.03
- p_th低下推定: ~3-5%（Fowler+ 2012 相関ノイズ解析ベース）
- p_L(d=7)への影響: 1.5-2倍増加（6.1×10⁻⁷ → 0.9-1.2×10⁻⁶）(MWPM基準)
- **ポンプRIN仕様**: −150 dB/Hz 以上を規定。Phase -1で実測検証要。
- **劣悪TA時（−140 dB/Hz以下）**: p_th,corr ≤ 0.80%、p_L(d=7) ≈ 2×10⁻⁶（製品要件超過リスク）

### 3.3 改善要因分析

| 改善要因 | σ_eff改善 | p_L改善(d=7) | 寄与 |
|---------|----------|-------------|------|
| σ_gen: 12→13-15dB (PPLN OPA) | +1-3dB生成 | — | **最大** |
| L_opt: 2→0.15-0.39dB (Option B + PIC) | 大幅損失低減 | — | **大** |
| σ_eff公式修正 (ビームスプリッタモデル) | 旧過大評価を修正 | — | 精度向上 |
| **Phase 2+ PIC (理想)** σ_eff=10.8dB | — | p_L≈6.1×10⁻⁷ (MWPM) / 7.0×10⁻⁶ (UF) | 製品要件達成(MWPM必須) |

*注記*: 旧版のσ_eff=11.5dBおよびp_L=4.4×10⁻⁷はdB減算による過大評価。erfc再計算の結果、Phase 2+ PIC構成(理論限界)でσ_eff=10.8dB、p_err≈1.0×10⁻³、p_L≈6.1×10⁻⁷(MWPM) / 7.0×10⁻⁶(UF)。製品デコーダはMWPM。

### 3.4 DV-FBQC方式との性能比較

| 指標 | **CV QD=0 Ph2+ (d=7)** | **CV QD=2 Ph2+ (d=5)** | DV v5.0 | DV R6 |
|------|---------|---|---------|---|
| p_L (UF/MWPM) | **≈7.0×10⁻⁶ / 6.1×10⁻⁷** | **~10⁻¹⁰** | ~10⁻⁶ | ~10⁻⁴ |
| 閾値マージン | +0.8dB (Noh&C, 理論限界) / +3.3dB (S-M, 理論限界) / +1.8dB (S-M, 現実的) | +6.0dB | +14pt | +7pt |
| 物理リソース | OPA8+Hom2 | +QD2+SNSPD4 | QD24+SNSPD500 | QD80+SNSPD3000 |
| 冷却 | **QDなし→不要** | PT205 1台 | PT205 1台 | RDK-415E |
| 論理qubit | **10-1,000+ (逐次)** | **10-1,000+ (逐次)** | 12-48 (同時) | 42 (同時) |
| **確実性** | **高** | **最高** | 中 | 中 |

---

## 4. スループット分析

### 4.1 TDMクロックレートと論理ゲートレート

| パラメータ | 旧 | **現行** | 根拠 |
|-----------|------|---------|------|
| TDMクロック | 10-100MHz | **100MHz** | パルスポンプOPA Option B |
| macronode列数 N_col | — | **98 (=2d²)** | d=7: 14×7=98 modes/round |
| 1シンドロームラウンド (d=7) | — | **980ns** | 14 steps × 7 cols × 10ns |
| QECサイクル (d=7) | 140ns-1.4μs | **~7μs** | 7ラウンド × 980ns |
| p_acc (単一モード acceptance) | — | **~93%** | δ=0.19√π窓。旧P_s記号廃止 |
| P_round (全モード同時高信頼) | — | **~10⁻³ (参考値)** | p_acc^98=0.93^98≈8×10⁻⁴。strict modeのみ関連 |
| **全モード利用レート (ベースライン)** | — | **~146kHz** | 1/T_QEC = 1/6.86μs。全モードsoft-info。製品スペック(L=0.27dB): p_L~3.3×10⁻⁴(MWPM) / ~4×10⁻³(UF) |
| strict postselectionレート (高品質) | — | **~120Hz** | P_round≈8×10⁻⁴(0.93^98), 146kHz×8×10⁻⁴≈117Hz, 概算~120Hz。p_L~6.1×10⁻⁷(MWPM, L=0.15dB, Δ→0) |
| **WDM 7ch 全モード利用 (製品スペック)** | — | **~1MHz (論理ゲート)** | 146kHz×7ch≈1.02MHz。全モードsoft-info、postselection不要。物理モードレートは100MHz×7ch=700Mmode/s |
| WDM 7ch strict (最高品質) | — | **~840Hz** | ~120Hz × 7ch |
| Tゲートレート (native T) | — | **= 論理ゲートレート** | Webster+ 2024, 蒸留不要 |

### 4.2 TDMの決定的優位性（不変）

| 論理qubit数 | DV-FBQC必要SNSPD | CV追加ハードウェア | CV追加コスト |
|------------|---------|---|---------|
| 10 | ~500 | なし | 0円 |
| 50 | ~2,500 | なし | 0円 |
| 100 | ~5,000 | なし | 0円 |
| 1,000 | ~50,000 | **なし** | **0円** |

---

## 5. 消費電力・サイズの性能スケーリング

| 論理qubit | CV消費電力 | CV筐体 | DV v5.0消費電力 | DV v5.0筐体 |
|----------|---------|---|---------|---|
| 12 | **~1.5kW** | 20U | 2.1kW | 60×40×50cm |
| 48 | **~1.5kW** | 20U | 4.2kW | ×2台 |
| 100 | **~1.5kW** | 20U | ~10kW | ×8台 |
| 1,000 | **~1.5kW** | **20U** | 不可能 | 不可能 |

**CV方式の消費電力~1.5kWはqubit数に対して定数（TDM方式の利点）。**

---

## 6. 新戦略: QDなしpure CV構成

### 6.1 QDなし構成のスペック

PPLN OPA + macronode TDMのみ（冷却不要）:

| 項目 | QDなし構成 | QD=2構成 |
|------|---------|---|
| ラック | 20U ハーフラック | 20U + PT205 |
| 重量 | **~30kg** | ~42kg |
| 消費電力 | **~185W** | ~1.5kW |
| 冷却 | **完全不要（室温）** | PT205 (8.6kg) |
| p_L (d=7, UF/MWPM) | ≈7.0×10⁻⁶ / 6.1×10⁻⁷ (Phase 2+ PIC) | ~10⁻¹⁴ |
| 論理qubit | 10-1,000+ | 10-1,000+ |
| GKP成功率 | 10⁻³ (postselection) | 10⁻¹ (QD支援) |
| 有効GKPレート | 100kHz | 10MHz |
| コスト | **~約2,055万円 (原価, 量産時)** | ~約2,925万円 (原価) |

### 6.2 QDなし構成の制約

- GKP生成レートが100kHz（QD支援の1/100）→ TDMの実効スループットが低下
- 大規模量子アルゴリズム（>100 Tゲート）は実行時間が長くなる
- しかし、QEC実証・小規模アルゴリズム・教育用途には十分

### 6.3 段階的QD追加戦略

```
Phase 0a: QDなし pure CV (30kg, 0.185kW, 約2,055万円原価)
    │   → QEC break-even実証
    │   → 投資家・ユーザーへのデモ
    │
Phase 0b: QD 2個追加 (42kg, ~1.5kW, 約2,925万円)
    │   → GKPレート100倍向上
    │   → 実用アルゴリズム実行
    │
Phase 1: QD 4個 (42kg, ~1.5kW, 約2,925万円)
    │   → 大規模FTQC
    │
Phase RT: Upconversion (8kg, 0.2kW, 約750万円)  ← 完全室温・開始後約9年年目標
```

---

## 7. 要Stimシミュレーション項目

| 優先度 | 項目 | 所要期間 | コスト | 意義 |
|--------|------|---------|--------|------------|
| **最高** | QDなしGKP+表面符号 p_L検証 | 2ヶ月 | 約300万円 | **QDなし構成の成立確認** |
| **最高** | macronode閾値 σ_th精密値 | 3ヶ月 | 約450万円 | Stafford-Menicucci値の独立検証 |
| 高 | QD支援の等価スクイージング精密値 | 3ヶ月 | 約300万円 | QD=2での+2.7dB検証 |
| 中 | ファイバ損失のCV性能への影響 | 2ヶ月 | 約150万円 | ノイズバジェットの実験検証 |
| 中 | マジック状態蒸留比（5:1改善可能性） | 2ヶ月 | 約150万円 | Tゲートレート向上 |

---

## 8. CV方式ネイティブ応用（キラーアプリ）

### 8.1 分子振動スペクトルシミュレーション（Phase 0, CV唯一）

**出典**: Nature Communications (2024), 大規模フォトニックネットワーク実証

CV方式の**光モードが分子振動モードに自然に対応**するため、分子振動スペクトルを指数的効率で計算可能。DV方式では原理的に非効率（ボソニック→qubit変換のオーバーヘッド）。

- **Tarosの8ch OPA → 8モード分子の振動スペクトル即座にデモ可能**
- ドゥシンスキー回転 = ビームスプリッタ操作（TarosのBS網で直接実装）
- 変位操作 = コヒーレント変位（EOMで実装済み）
- フランク・コンドン因子の量子サンプリング → GBS（ガウシアンボソンサンプリング）で実行

### 8.2 量子重力シミュレーション（Phase 0-1）

CVボソニックモードが量子場の量子に直接対応。詳細は `01_system-architecture.md` Appendix B参照。
- LQGスピンフォーム: Phase 0 (数量子ビット)
- SYK/ワームホール: Phase 1 (10-50論理量子ビット)

### 8.3 金融モンテカルロ（Phase 1+）

GBS（ガウシアンボソンサンプリング）による確率分布サンプリングはCV方式のネイティブ計算。ポートフォリオ最適化やオプション価格付けに量子優位性を示す可能性。

### 8.4 コヒーレントイジングマシン（CIM）モード（Phase -1, 追加HWゼロ）

**出典**: Science Advances (NTT 100,000スピンCIM), Nature Comms 2026 (Kerr CIM), Light: Sci & Apps 2026

**TarosハードウェアはCIMとほぼ同一構成**。NTTのCIMは「PPLN導波路 + ファイバリング共振器 @ 1550nm」で構成されており、Tarosの「PPLN OPA + 200m PMF遅延線 @ 1550nm」と物理的に等価。

- **切替方法**: OPAポンプ強度を閾値以下に下げるだけでCIMモードに移行（SW/FPGA変更のみ）
- **容量**: 200m遅延線 → ~2,000パルス（100MHz）のイジングスピンを格納可能
- **応用**: 組合せ最適化（Max-Cut, 独立集合問題, 交通最適化, スケジューリング）
- **FPGAフィードバック**: 既存の測定→FPGA→EOMループをCIMの測定フィードバック結合に転用
- **ビジネス価値**: FTQC到達前（Phase -1/0）で組合せ最適化デモを即座に提供可能

### 8.5 量子リザーバコンピューティング（Phase -1, 追加HWゼロ）

**出典**: Nature Photonics 2026 (Paparelle et al.), Nature Comms 2025 (スケーラブルフォトニックリザーバ)

**Tarosのアーキテクチャそのものがリザーバコンピュータ**。OPA → PMF遅延 → ホモダイン → FPGA → EOM のループ構造は、量子リザーバ計算の標準的アーキテクチャと完全に一致。

- **実装**: SW変更のみ。入力信号をEOMで注入し、ホモダイン出力を読み出し層として利用
- **14bit soft-info**: リザーバの表現力を大幅に向上（DV方式のbinary出力では不可能な高精度）
- **WDM 8ch**: 8並列リザーバチャネルとして同時に異なるタスクを実行可能
- **応用**: 時系列予測、異常検知、カオス信号予測、音声認識
- **フォールトトレランス不要**: 生のスクイーズド光+ホモダイン測定で動作。QEC前の即時デモ

### 8.6 Taros 3-in-1コンセプト

Tarosは**単一ハードウェアで3つの計算パラダイムを提供**する世界初のプラットフォーム:

```
┌─────────────────────────────────────────────────────┐
│              Taros 3-in-1 Quantum Platform           │
│                                                       │
│  Mode 1: FTQC (GKP+表面符号)                         │
│    → 量子化学、量子重力シミュレーション                  │
│    → Phase 0+ (QEC break-even後)                     │
│                                                       │
│  Mode 2: CIM (閾値以下OPA)                            │
│    → 組合せ最適化、スケジューリング                      │
│    → Phase -1 即時（SW変更のみ）                       │
│                                                       │
│  Mode 3: QRC (リザーバ計算)                            │
│    → 時系列予測、異常検知、パターン認識                  │
│    → Phase -1 即時（SW変更のみ）                       │
│                                                       │
│  共通HW: OPA + PMF遅延線 + ホモダイン + FPGA + EOM     │
└─────────────────────────────────────────────────────┘
```

---

*本文書はσ_eff公式修正（ビームスプリッタモデル+V_non-loss=0.010 SNU）、Option B推奨、フェーズ別ロードマップ、erfc再計算を反映。Phase 2+ PIC構成(13dB, L=0.15dB)でσ_eff=10.8dB、p_err≈1.0×10⁻³、p_L(d=7, MWPM) ≈ 6.1×10⁻⁷。UF d=7では7.0×10⁻⁶>10⁻⁶のため製品デコーダはMWPM。旧値σ_eff=11.5dB（dB減算）は廃止。WDM並列化(5-8ch)が製品ゲートレート達成に必須。*
